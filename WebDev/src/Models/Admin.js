class clsAdmin {
     Email;
     AdminType;
     Local;
     Name;
     Password;

    constructor(email, type, local,
      name, password){
      this.Email = email,
      this.Type = type,
      this.Local = local,
      this.Name = name,
      this.Password = password;
    }
  }
