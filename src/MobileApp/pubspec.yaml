name: ReachUp
description: Mobile application for indoor location, provides blind and visually impaired an interaction experience in the mall, using voice tools the users can search by stores, bathrooms, restaurants and so on. Besides that, the application offers tools for shopkeepers publish their promotions or communicated and mall managers receive a user visit reports by the mall.

publish_to: 'none' 

version: 1.0.7+7

environment:
  sdk: "2.19.6"

dependencies:
  flutter:
    sdk: flutter
  dio: 4.0.1
  mobx: ^2.3.0+1
  flutter_mobx: ^2.2.0+2
  provider: ^6.1.1
  font_awesome_flutter: ^9.2.0
  flutter_beacon: ^0.5.1

  photo_view: ^0.14.0
  path_provider_windows: ^2.2.1 
  flutter_tts: ^3.8.5
  sensors: ^2.0.3
  flutter_compass: ^0.8.0
  gesture_x_detector: ^1.1.1

  lottie: ^2.3.2

  cupertino_icons: ^1.0.6
  http: ^0.13.6
  flutter_signin_button: ^2.0.0
  async: ^2.10.0
  flutter_easyloading: ^3.0.5
  cached_network_image: ^3.2.3

  intl: ^0.18.1
  flutter_rating_bar: ^4.0.1
  toggle_switch: ^2.1.0
  flutter_slidable: ^3.0.1
  get_storage: ^2.1.1
  get: ^4.6.5
  awesome_circular_chart: ^1.0.0

  
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.3.3
  mobx_codegen: ^2.3.0
  flutter_launcher_icons: ^0.13.1
 
flutter:
  assets:
    #Animations
    - assets/animations/user-location.json
    - assets/animations/pin-pointer.json
    - assets/animations/search.json
    - assets/animations/pin.json

    #Images
    - assets/images/map/floor0.jpg
    - assets/images/map/floor1.jpg
    - assets/images/bkg/bkg_initial_screen.jpg

    - assets/images/icons/widgets/user-location.png
    - assets/images/icons/launch/ReachUp.png

    #Config
    - assets/config/global/config-global.json
    - assets/map/map.json


  uses-material-design: true
